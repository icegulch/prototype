---
permalink: /
layout: base.liquid
---

{% for post in posts %}
  <time>
    {{ post.created_at | date: "%a, %b %-d '%y @ %H:%M" }}
  </time>
  {{ post.message | markdownify | prependCloudinaryURL }}
{% endfor %}
